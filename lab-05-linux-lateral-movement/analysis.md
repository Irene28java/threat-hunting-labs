Tras comprometer la cuenta alice, el atacante reutiliza su clave SSH
para autenticarse como bob debido a una mala gestión de authorized_keys.

El acceso se realiza sin contraseña, sin uso de sudo y sin exploits,
lo que lo convierte en un método de movimiento lateral silencioso
y difícil de detectar en entornos Linux.
