# Lab 01 – Linux SSH Bruteforce Detection

## Objective
Detect and analyze brute force attempts against an SSH service using Linux authentication logs.

## Environment
- OS: Ubuntu 22.04 (WSL2)
- Service: OpenSSH
- Log file: /var/log/auth.log

## Scenario
Multiple failed SSH authentication attempts were generated against a non-existent user account to simulate a brute force attack.
